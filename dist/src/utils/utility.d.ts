declare class Utility {
    static generateRandomString(length?: number): string;
    static generateSlugFromText(text: string): string;
    static shuffleList(list: any[]): any[];
}
export default Utility;
